#include <stdio.h>
/*
 *main - main entry
 *return : it will return (0) if successful
 *puts - this is the output statement
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
