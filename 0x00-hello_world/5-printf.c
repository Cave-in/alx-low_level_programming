#include <stdio.h>
/*
 * main - Entry point
 * puts - outputs the statement
 * return : always 0  successful
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
